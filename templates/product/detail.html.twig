{% extends 'base.html.twig' %}

{% block title %}Detail produktu{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-body">
            <h1 class="card-title mb-3">{{ product.name }}</h1>
            <p class="card-text">{{ product.description }}</p>
            <p class="fs-5 fw-semibold">Cena: <span class="text-primary">{{ product.price }} {{ product.currency.code }}</span></p>
            <a href="{{ path('product_list') }}" class="btn btn-outline-secondary mt-3">
                &larr; Zpět na seznam
            </a>
        </div>
    </div>
</div>
{% endblock %}
